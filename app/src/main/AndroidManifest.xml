<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ranieri.bodegaweb">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListaProdutosActivity" />
        <activity android:name=".DetalProdutosActivity" />
        <activity android:name=".EditarProdutosActivity" />
        <activity android:name=".EditStockActivity" />
        <activity android:name=".ListaCategoriasActivity" />
        <activity android:name=".ConfiguracoesActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".ListOrderItemsActivity" />
        <activity android:name=".ProviderActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".CheckingLoginActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
    </application>

</manifest>